<?xml version="1.0" encoding="utf-8"?>
<!-- Compact overlay panel for the VoiceInteractionSession.
     Renders as a bottom-anchored card over the current app. -->
<FrameLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@android:color/transparent">

    <!-- Bottom card -->
    <LinearLayout
        android:id="@+id/overlay_card"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_gravity="bottom"
        android:orientation="vertical"
        android:padding="20dp"
        android:layout_marginStart="12dp"
        android:layout_marginEnd="12dp"
        android:layout_marginBottom="24dp"
        android:background="@drawable/overlay_card_bg"
        android:elevation="8dp">

        <!-- Status text -->
        <TextView
            android:id="@+id/overlay_status"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Listening..."
            android:textSize="16sp"
            android:textColor="#FFFFFFFF"
            android:fontFamily="sans-serif-medium"
            android:gravity="center"
            android:paddingBottom="12dp" />

        <!-- Transcript text (what the user said) -->
        <TextView
            android:id="@+id/overlay_transcript"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text=""
            android:textSize="14sp"
            android:textColor="#B3FFFFFF"
            android:gravity="center"
            android:visibility="gone"
            android:paddingBottom="12dp" />

        <!-- Result chip (action result) -->
        <TextView
            android:id="@+id/overlay_result"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text=""
            android:textSize="14sp"
            android:textColor="#FFFFFFFF"
            android:gravity="center"
            android:visibility="gone"
            android:paddingBottom="8dp" />

        <!-- Mic button row -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:gravity="center"
            android:orientation="horizontal">

            <ImageButton
                android:id="@+id/overlay_mic_btn"
                android:layout_width="56dp"
                android:layout_height="56dp"
                android:src="@android:drawable/ic_btn_speak_now"
                android:background="@drawable/mic_button_bg"
                android:contentDescription="Speak"
                android:scaleType="centerInside"
                android:padding="12dp" />

        </LinearLayout>

    </LinearLayout>

</FrameLayout>
